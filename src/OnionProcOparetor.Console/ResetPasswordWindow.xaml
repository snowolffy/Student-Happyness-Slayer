<Window x:Class="OnionProcOparetor.Console.ResetPasswordWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Reset Password"
        Height="280" Width="420"
        WindowStartupLocation="CenterOwner"
        Background="#0F1115"
        Foreground="#E8E9EB"
        ResizeMode="NoResize">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="16"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="16"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" x:Name="TitleText" FontSize="16" FontWeight="Bold"/>

        <Border Grid.Row="2" Style="{StaticResource Card}" Background="{StaticResource InputBgBrush}" Padding="14">
            <StackPanel>
                <TextBlock Text="NEW PASSWORD" FontSize="10" Foreground="#8A8F98"/>
                <TextBlock Text="At least 8 characters."
                           FontSize="10" Foreground="#8A8F98" Margin="0,2,0,8" TextWrapping="Wrap"/>
                <PasswordBox x:Name="NewPasswordBox" Width="250" HorizontalAlignment="Left"/>
            </StackPanel>
        </Border>

        <TextBlock Grid.Row="4" x:Name="StatusText" FontSize="11" Foreground="#8A8F98" TextWrapping="Wrap"/>

        <StackPanel Grid.Row="6" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="RESET" Style="{StaticResource PrimaryButton}"
                    Margin="0,0,8,0" Click="ResetButton_Click"/>
            <Button Content="CANCEL" Style="{StaticResource SmallActionButton}"
                    Click="CancelButton_Click"/>
        </StackPanel>
    </Grid>
</Window>